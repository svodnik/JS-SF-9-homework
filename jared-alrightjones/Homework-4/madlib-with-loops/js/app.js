// provided code

var startupX = ['Uber', 'Google', 'Amazon', 'Apple', 'Facebook', 'Twitter'];
var startupY = ['Slack', 'GitHub', 'Tesla', 'Hyperloop', 'Harvest'];

var random1 = Math.floor((Math.random() * startupX.length));
var random2 = Math.floor((Math.random() * startupY.length));

// create something

var greatIdea;
const createBtn = document.querySelector('#create'); 
const outputField = document.querySelector('#xForY');
createBtn.addEventListener('click' outputText)

function outputText() {
	greatIdea = 'A startup that is ' + startupX[random1] + ', but for ' + startupY[random2];
	startup.innerHTML = startupIdea;
}

// save something


// print something
